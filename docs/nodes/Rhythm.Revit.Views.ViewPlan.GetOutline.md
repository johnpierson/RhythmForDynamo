## In Depth

`ViewPlan.GetOutline(view)`

This node will get the bounds of the view in paper space (in feet).

The inputs are:

- `view` (_Element_) — The view to get outline from.

Returns `outline` (_list of list of Curve_) — The bounds of the view in paper space (in feet).

Search terms: `view`, `outline`, `rhythm`.

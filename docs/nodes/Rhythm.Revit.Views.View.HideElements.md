## In Depth

`View.HideElements(view, elementsToHide)`

Hide the given elements in the given view.

The inputs are:

- `view` (_View_) — The view to hide elements in.
- `elementsToHide` (_list of Element_) — The elements to hide.

Returns nothing.

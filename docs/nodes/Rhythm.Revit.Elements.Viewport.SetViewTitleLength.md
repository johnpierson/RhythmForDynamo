## In Depth

`Viewport.SetViewTitleLength(viewport, length)`

Set a viewport's title length. Revit 2022+

The inputs are:

- `viewport` (_Element_) — The target viewport.
- `length` (_number_) — The length to set it to.

Returns nothing.

Found in the library under **Actions**.

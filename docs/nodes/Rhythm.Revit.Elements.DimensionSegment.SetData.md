## In Depth

`DimensionSegment.SetData(segment, above: Rhythm.Utilities.MiscUtils.GetNull(), below: Rhythm.Utilities.MiscUtils.GetNull(), prefix: Rhythm.Utilities.MiscUtils.GetNull(), suffix: Rhythm.Utilities.MiscUtils.GetNull())`

Set the data given the inputs.

The inputs are:

- `segment` (_DimensionSegment_) — The dimension segment
- `above` (_string_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Above value (optional)
- `below` (_string_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Below value (optional)
- `prefix` (_string_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Prefix value (optional)
- `suffix` (_string_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Suffix value (optional)

Returns nothing.

Found in the library under **Actions**.

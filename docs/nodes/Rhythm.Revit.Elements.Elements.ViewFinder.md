## In Depth

`Elements.ViewFinder(element)`

This finds all the views an element appears in. Note: "Appears in" means that if it appears when you do a category collector, that counts.

The inputs are:

- `element` (_Element_) — The element to analyze.

Returns `views` (_list of Element_) — The views this element appears in.

Found in the library under **Actions**.

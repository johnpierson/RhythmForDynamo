## In Depth

`Element.Creator(element)`

This node will output the username of the creator of the element if it is available. Keep in mind this only works with workshared documents!

The inputs are:

- `element` (_Element_) — The element to check.

Returns `creatorName` (_string_) — The username of the creator.

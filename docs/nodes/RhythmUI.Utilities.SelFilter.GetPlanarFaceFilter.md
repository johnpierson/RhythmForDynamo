## In Depth

`SelFilter.GetPlanarFaceFilter(doc)`

Creates a selection filter that will let only PlanarFace-References pass

The inputs are:

- `doc` (_Document_) — _Not documented yet._

Returns `result` (_IReferenceSelectionFilter_).

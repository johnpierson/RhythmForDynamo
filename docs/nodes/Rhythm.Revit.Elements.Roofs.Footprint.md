## In Depth

`Roofs.Footprint(roof)`

Retrieve the footprint of any roof element

The inputs are:

- `roof` (_Element_) — The roof to extract footprint for.

Returns `footprint` (_list of Curve_) — The roof footprint as a polygon

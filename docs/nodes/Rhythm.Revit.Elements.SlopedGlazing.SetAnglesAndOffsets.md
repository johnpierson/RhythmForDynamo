## In Depth

`SlopedGlazing.SetAnglesAndOffsets(slopedGlazing, angle1: Rhythm.Utilities.MiscUtils.GetNull(), angle2: Rhythm.Utilities.MiscUtils.GetNull(), offset1: Rhythm.Utilities.MiscUtils.GetNull(), offset2: Rhythm.Utilities.MiscUtils.GetNull())`

Set the offset parameters

The inputs are:

- `slopedGlazing` (_Roof_) — _Not documented yet._
- `angle1` (_Nullable of number_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Value between -89 and 89
- `angle2` (_Nullable of number_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Value between -89 and 89
- `offset1` (_Nullable of number_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Value to offset side 1 by
- `offset2` (_Nullable of number_, defaults to `Rhythm.Utilities.MiscUtils.GetNull()`) — Value to offset side 2 by

Returns `result` (_Roof_).

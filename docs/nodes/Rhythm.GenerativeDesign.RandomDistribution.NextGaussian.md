## In Depth

`RandomDistribution.NextGaussian(standardDeviation: 1)`

Return a number in the range (-1, +1) with a Normal distributed probability using the Marsaglia polar method.

The inputs are:

- `standardDeviation` (_number_, defaults to `1`) — It is a measure of the amount of variation or dispersion of the values.

Returns `result` (_number_).

## In Depth

`Documents.DynamoDocumentToDbDocument(dynamoDocument)`

Convert a Dynamo document to the db kind.

The inputs are:

- `dynamoDocument` (_Document_) — The Dynamo document.

Returns `dynamoDocument` (_Document_) — The converted document as Autodesk.Revit.DB.Document

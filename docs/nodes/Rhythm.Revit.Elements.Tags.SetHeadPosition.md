## In Depth

`Tags.SetHeadPosition(tag, location)`

This will attempt to set the head position of the tag.

The inputs are:

- `tag` (_Tag_) — The tag to set head position of.
- `location` (_Point_) — The new location for the head.

Returns nothing.

Found in the library under **Actions**.

Search terms: `Tag`, `Tag.Location`.

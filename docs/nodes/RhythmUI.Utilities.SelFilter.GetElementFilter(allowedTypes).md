## In Depth

`SelFilter.GetElementFilter(allowedTypes)`

Creates a selection filter, which elements of any Type in the collection will pass

The inputs are:

- `allowedTypes` (_list of Type_) — _Not documented yet._

Returns `result` (_IElementSelectionFilter_).

## In Depth

`SelFilter.GetFaceNormalFilter(doc, normal, acceptParallel: false)`

Creates a selection filter that will let faces pass, if their normal vector at (0/0) is codirectional or parallel to the given normal vector

The inputs are:

- `doc` (_Document_) — _Not documented yet._
- `normal` (_XYZ_) — _Not documented yet._
- `acceptParallel` (_boolean_, defaults to `false`) — _Not documented yet._

Returns `result` (_IReferenceSelectionFilter_).

## In Depth

`RibbonTab.SetName(tab, name)`

This will rename a tab given a new name.

The inputs are:

- `tab` (_RibbonTab_) — The tab.
- `name` (_string_) — The new name.

Returns `result` (_string_) — The result..

Search terms: `Ribbon.RenameTab`.

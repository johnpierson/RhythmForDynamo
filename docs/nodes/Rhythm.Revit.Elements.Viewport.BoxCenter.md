## In Depth

`Viewport.BoxCenter(viewport)`

This node will retrieve the viewport's box center.

The inputs are:

- `viewport` (_Element_) — The viewport to set.

Returns `boxCenter` (_Point_) — The viewport's box center as a point.

Found in the library under **Query**.

Search terms: `viewport`.

## In Depth

`Tags.GetLeaderElbow(tag)`

The position of the elbow of the tag's leader.

The inputs are:

- `tag` (_Tag_) — The tag to get leader elbow position from.

Returns `point` (_object_) — The tag's leader elbow position as a point.

Found in the library under **Query**.

Search terms: `Tag`, `Tag.Location`.

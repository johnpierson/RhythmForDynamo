## In Depth

`RibbonTab.SetEnabled(tab, toggle)`

This will enable or disable the given tab.

The inputs are:

- `tab` (_RibbonTab_) — The tab name to modify.
- `toggle` (_boolean_) — True or false for tab visibility.

Returns `result` (_string_) — The result..

Search terms: `Ribbon.RenameTab`.

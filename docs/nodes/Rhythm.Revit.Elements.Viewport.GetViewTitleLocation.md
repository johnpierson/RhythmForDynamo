## In Depth

`Viewport.GetViewTitleLocation(viewport)`

Get a viewport's title location (relative to the boundary of the view) Revit 2022+.

The inputs are:

- `viewport` (_Element_) — The target viewport.

Returns `result` (_Point_).

Found in the library under **Actions**.

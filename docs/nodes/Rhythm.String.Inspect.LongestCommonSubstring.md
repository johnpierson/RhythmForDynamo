## In Depth

`Inspect.LongestCommonSubstring(string1, string2)`

Find the longest common substring between two strings.

The inputs are:

- `string1` (_string_) — First one to compare.
- `string2` (_string_) — Second one to compare.

Returns `longestCommonSubstring` (_string_) — The longest common substring.

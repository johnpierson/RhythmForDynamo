## In Depth

`SelFilter.GetElementFilter(id, ids)`

Creates a selection filter that will let pass only the elements defined by the ids

The inputs are:

- `id` (_ElementId_) — _Not documented yet._
- `ids` (_list of ElementId_) — _Not documented yet._

Returns `result` (_IElementSelectionFilter_).

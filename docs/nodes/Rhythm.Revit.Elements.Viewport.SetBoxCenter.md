## In Depth

`Viewport.SetBoxCenter(viewport, point)`

This node will set the viewport's box center given the point.

The inputs are:

- `viewport` (_Element_) — The viewport to set.
- `point` (_Point_) — The point to use.

Returns nothing.

Found in the library under **Actions**.

Search terms: `viewport`.

## In Depth

`Numbers.ToHeadingArrow(number)`

Convert the input numbers into headings: ↑, →, ↓, ←. Made possible with Humanizer (https://github.com/Humanizr/Humanizer)

The inputs are:

- `number` (_number_) — _Not documented yet._

Returns `result` (_string_).

Found in the library under **Actions**.

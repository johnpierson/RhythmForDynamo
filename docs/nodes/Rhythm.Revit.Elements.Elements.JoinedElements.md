## In Depth

`Elements.JoinedElements(element)`

This node will report what elements are joined to the input element.

The inputs are:

- `element` (_Element_) — The element to check.

Returns `joinedElements` (_list of Element_) — The joined elements.

Found in the library under **Query**.

Search terms: `Element.GetJoinedElements`, `JoinedElements`.

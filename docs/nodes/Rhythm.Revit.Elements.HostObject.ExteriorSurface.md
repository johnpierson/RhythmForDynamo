## In Depth

`HostObject.ExteriorSurface(hostObject)`

This node will return the exterior face or faces for the input host object. This particular method works for walls.

The inputs are:

- `hostObject` (_Element_) — The host object to retrieve exterior faces for.

Returns `result` (_list of list of Surface_).

Found in the library under **Query**.

## In Depth

`Areas.AreaScheme(area)`

Get the input area's scheme.

The inputs are:

- `area` (_Element_) — The area to check.

Returns `areaScheme` (_Element_).

Found in the library under **Query**.

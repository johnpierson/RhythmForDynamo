## In Depth

`Ceiling.GetGridLines(ceiling, includeBoundary: false)`

Returns ceiling grid lines, with the option to return the boundary as well.

The inputs are:

- `ceiling` (_Element_) — The ceiling to extract grids from
- `includeBoundary` (_boolean_, defaults to `false`) — Extract the boundary?

Returns `ceilingGrids` (_list of Curve_).

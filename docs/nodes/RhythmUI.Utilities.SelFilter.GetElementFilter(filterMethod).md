## In Depth

`SelFilter.GetElementFilter(filterMethod)`

Creates a selection filter that will use the "filterMethod" to filter the elements

The inputs are:

- `filterMethod` (_Func of Element and boolean_) — _Not documented yet._

Returns `result` (_IElementSelectionFilter_).

## In Depth

`Tags.GetLeaderEnd(tag)`

The position of the leader end for a tag using free end leader behavior.

The inputs are:

- `tag` (_Tag_) — The tag to get leader end position from.

Returns `point` (_object_) — The tag's leader end position as a point.

Found in the library under **Query**.

Search terms: `Tag`, `Tag.Location`.

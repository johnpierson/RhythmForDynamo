## In Depth

`View.ConvertToIndependent(dependentView)`

This node will convert a dependent view to an independent.

The inputs are:

- `dependentView` (_Element_) — The view to convert to independent.

Returns `independentView` (_list of object_) — The overidden view.

Search terms: `view`, `dependent`, `rhythm`.

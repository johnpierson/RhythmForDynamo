## In Depth

`Categories.LineColor(category)`

Get the category line color as RGB string..

The inputs are:

- `category` (_list of Category_) — _Not documented yet._

The outputs are:

- `lineColorString` — The category line color as a string.
- `lineColor` — The category line color as a usable Dynamo color.

Found in the library under **Query**.

Search terms: `Categories.LineColor`.

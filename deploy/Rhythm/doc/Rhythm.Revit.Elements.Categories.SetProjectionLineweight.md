## In Depth

`Categories.SetProjectionLineweight(category, penWeight)`

Set the category projection lineweight.

The inputs are:

- `category` (_Category_) — _Not documented yet._
- `penWeight` (_integer_) — _Not documented yet._

Returns `category` (_Category_) — The category

Found in the library under **Action**.

Search terms: `Categories.ProjectionLineweight`.

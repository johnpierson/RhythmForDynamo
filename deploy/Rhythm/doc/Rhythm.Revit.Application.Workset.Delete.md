## In Depth

`Workset.Delete(workset)`

Deletes a user workset from the active document.

The inputs are:

- `workset` (_Workset_) — The workset to delete.

Returns `success` (_boolean_) — Whether the deletion succeeded.

Found in the library under **Actions**.

Search terms: `workset`, `delete`, `remove`, `workshared`, `rhythm`.

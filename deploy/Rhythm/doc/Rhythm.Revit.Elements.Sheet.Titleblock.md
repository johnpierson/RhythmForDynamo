## In Depth

`Sheet.Titleblock(viewSheet)`

This node will grab the titleblock from the given sheet.

The inputs are:

- `viewSheet` (_Sheet_) — The sheet to get titleblock from.

Returns `titleblock` (_list of Element_) — The sheet's titleblock.

Found in the library under **Query**.

Search terms: `sheet`, `sheets`, `titleblock`.

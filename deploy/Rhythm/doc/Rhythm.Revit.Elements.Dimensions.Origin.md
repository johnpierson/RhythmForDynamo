## In Depth

`Dimensions.Origin(dimension)`

This node will return the origin of the dimension. If it is a multi-segment dimension it will output all of the pieces.

The inputs are:

- `dimension` (_Dimension_) — The dimension.

Returns `origin` (_list of Point_) — The dimension origin.

Found in the library under **Query**.

Search terms: `dimension.Origin`.

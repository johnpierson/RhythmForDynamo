## In Depth

`SelFilter.GetReferenceFilter(filterMethod)`

Creates a selection filter that will use the "filterMethod" to filter the references

The inputs are:

- `filterMethod` (_Func of Reference and XYZ and boolean_) — _Not documented yet._

Returns `result` (_IReferenceSelectionFilter_).

## In Depth

`Tags.TagText(tag)`

This will return the tag's text value

The inputs are:

- `tag` (_Element_) — The tag to get text of.

Returns `result` (_string_).

Found in the library under **Query**.

## In Depth

`Leaders.GetLeaderElbow(leader)`

This will get the position of the leader's elbow. Note: Obtain the leader element from the text note with TextNote.GetLeaders

The inputs are:

- `leader` (_Leader_) — The leader to get the elbow position of

Returns `result` (_Point_).

Found in the library under **Query**.

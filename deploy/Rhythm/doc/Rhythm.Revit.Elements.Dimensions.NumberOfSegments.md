## In Depth

`Dimensions.NumberOfSegments(dimension)`

This node will return the number of segments comprising the multi segment dimension.

The inputs are:

- `dimension` (_list of Dimension_) — Multi segment dimension.

Returns `numberOfSegments` (_list of integer_) — The amount of segments.

Found in the library under **Query**.

Search terms: `dimension.Segments`.

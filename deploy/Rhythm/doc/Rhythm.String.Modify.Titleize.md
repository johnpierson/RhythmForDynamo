## In Depth

`Modify.Titleize(str)`

Titleize converts the input words to Title casing Made possible with Humanizer (https://github.com/Humanizr/Humanizer)

The inputs are:

- `str` (_string_) — The string to titleize.

Returns `str` (_string_) — The string in it's title form.

Found in the library under **Actions**.

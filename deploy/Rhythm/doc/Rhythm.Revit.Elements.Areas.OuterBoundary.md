## In Depth

`Areas.OuterBoundary(area)`

This node will retrieve the area's outermost boundary.

The inputs are:

- `area` (_Element_) — The area to extract outermost boundary from.

Returns `polygon` (_Polygon_) — The outermost polygon.

Found in the library under **Query**.

Search terms: `Area.OuterBoundary`.

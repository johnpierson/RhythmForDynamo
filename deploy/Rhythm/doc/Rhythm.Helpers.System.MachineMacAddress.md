## In Depth

`System.MachineMacAddress(refresh: true)`

Returns the current computer's mac address.

The inputs are:

- `refresh` (_boolean_, defaults to `true`) — Optional toggle to refresh the node

Returns `result` (_string_).

## In Depth

`Ceiling.DefaultCeilingType()`

Collect the first ceiling type available. Revit 2022+

Returns `ceilingType` (_Element_) — The first (default) ceiling type.

Found in the library under **Query**.

## In Depth

`RibbonTab.SetVisibility(tab, toggle)`

This will hide or show the given tab.

The inputs are:

- `tab` (_RibbonTab_) — The tab to modify.
- `toggle` (_boolean_) — True or false for tab visibility.

Returns `result` (_string_) — The result..

Search terms: `Ribbon.RenameTab`.

## In Depth

`View.GetOrderedFilters(view)`

Revit 2021 - Returns the filters in order for the given view.

The inputs are:

- `view` (_View_) — The view to check the filter on.

Returns `result` (_list of Element_).

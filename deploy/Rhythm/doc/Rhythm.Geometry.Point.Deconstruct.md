## In Depth

`Point.Deconstruct(point)`

Return the XYZ values of a given point.

The inputs are:

- `point` (_Point_) — The point to return values for.

The outputs are:

- `x` — _Not documented yet._
- `y` — _Not documented yet._
- `z` — _Not documented yet._

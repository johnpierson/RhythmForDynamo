## In Depth

`Levels.HasView(level)`

Check to see if the level has a view created for it.😎

The inputs are:

- `level` (_Level_) — The level to check.

Returns `result` (_boolean_) — Does it have a level?

Found in the library under **Query**.

Search terms: `Level.HasView`.

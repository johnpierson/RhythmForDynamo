## In Depth

`View.IsFilterEnabled(view, viewFilter)`

Revit 2021 - Checks if a view filter is enabled in the given view.

The inputs are:

- `view` (_View_) — The view to check the filter on.
- `viewFilter` (_Element_) — The view filter to check.

Returns `bool` (_boolean_) — Is it enabled?

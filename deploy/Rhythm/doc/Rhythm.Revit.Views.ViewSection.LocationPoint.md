## In Depth

`ViewSection.LocationPoint(view)`

Retrieve the input view's origin, (if available).

The inputs are:

- `view` (_list of Element_) — The view to get origin of.

Returns `locationPoint` (_list of Point_) — The origin of the view. Also, the origin of a plan view is not meaningful.

Search terms: `viewsection.locationpoint`.

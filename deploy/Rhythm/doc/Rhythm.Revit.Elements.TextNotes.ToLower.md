## In Depth

`TextNotes.ToLower(textNote)`

This node will convert the text note to lower with formatting.

The inputs are:

- `textNote` (_Element_) — The text note to convert.

Returns `textNote` (_Element_) — The converted text note.

Found in the library under **Actions**.

Search terms: `textnote`, `tolower`, `rhythm`.

## In Depth

`SelFilter.GetElementFilter(type, types)`

Creates a selection filter, which elements of any of the given Types  will pass

The inputs are:

- `type` (_Type_) — _Not documented yet._
- `types` (_list of Type_) — _Not documented yet._

Returns `result` (_IElementSelectionFilter_).

## In Depth

`Image.ConvertToBase64(imagePath)`

Converts an input image to a base64 (string) representation.

The inputs are:

- `imagePath` (_string_) — The image to convert.

Returns `base64String` (_string_).

## In Depth

`RibbonTab.Name(tab)`

This will get the tab's name.

The inputs are:

- `tab` (_RibbonTab_) — The original tab name.

Returns `name` (_string_) — The result..

Search terms: `Ribbon.RenameTab`.

## In Depth

`Areas.Boundaries(area)`

This node will retrieve the area's boundaries. The first list is typically the outermost boundaries.

The inputs are:

- `area` (_Element_) — The area to extract curves from.

Returns `boundaries` (_list of list of Curve_) — The boundaries.

Found in the library under **Query**.

Search terms: `Area.Boundaries`.

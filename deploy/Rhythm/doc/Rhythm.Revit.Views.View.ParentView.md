## In Depth

`View.ParentView(view)`

Retrieve the input dependent view's parent, (if available).

The inputs are:

- `view` (_Element_) — The view to get parent of.

Returns `parentView` (_object_) — The parent view.

Search terms: `dependent`, `parent`, `rhythm`.

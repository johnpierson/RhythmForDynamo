## In Depth

`Modifiers.Rotate(rotation)`

This will rotate your ribbon. Seriously.

The inputs are:

- `rotation` (_integer_) — The amount to rotate the ribbon.

Returns `rotation` (_integer_) — The rotated amount.

## In Depth

`Tags.GetHeadPosition(tag)`

The position of the head of tag in model coordinates (if available).

The inputs are:

- `tag` (_Element_) — The tag to get head position from.

Returns `point` (_Point_) — The tag's head position as a point.

Found in the library under **Query**.

Search terms: `Tag`, `Tag.Location`.

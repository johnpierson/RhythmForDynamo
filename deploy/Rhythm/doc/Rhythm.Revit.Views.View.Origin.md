## In Depth

`View.Origin(view)`

Retrieve the input view's origin, (if available).

The inputs are:

- `view` (_Element_) — The view to get origin of.

Returns `origin` (_object_) — The origin of the view. Also, the origin of a plan view is not meaningful.

Search terms: `dependent`, `parent`, `rhythm`.

## In Depth

`Documents.DbDocumentToDynamoDocument(dbDocument)`

Convert a db document to the Dynamo kind.

The inputs are:

- `dbDocument` (_Document_) — The background opened DB document.

Returns `dynamoDocument` (_Document_) — The converted document as Revit.Application.Document

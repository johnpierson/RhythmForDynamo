## In Depth

`Element.LastChangedBy(element)`

This node will output the username of the person who last changed the element if it is available. Keep in mind this only works with workshared documents!

The inputs are:

- `element` (_Element_) — The element to check.

Returns `lastChangedBy` (_string_) — The username of the person who last changed the element.

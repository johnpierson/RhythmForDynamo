## In Depth

`Workset.Create(name)`

Creates a new user workset in the active document.

The inputs are:

- `name` (_string_) — The name of the workset to create.

Returns `workset` (_Workset_) — The newly created workset.

Found in the library under **Actions**.

Search terms: `workset`, `create`, `workshared`, `rhythm`.

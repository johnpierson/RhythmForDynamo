## In Depth

`TextNotes.ToUpper(textNote)`

This node will convert the text note to upper with formatting.

The inputs are:

- `textNote` (_Element_) — The text note to convert.

Returns `textNote` (_Element_) — The converted text note.

Found in the library under **Actions**.

Search terms: `textnote`, `toupper`, `rhythm`.

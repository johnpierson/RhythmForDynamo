## In Depth

`Categories.BuiltInCategoryName(category)`

Get the built in category name. Useful for troubleshooting.

The inputs are:

- `category` (_Category_) — The category

Returns `builtInCategoryName` (_string_).

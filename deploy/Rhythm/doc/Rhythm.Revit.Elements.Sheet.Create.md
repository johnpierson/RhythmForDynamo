## In Depth

`Sheet.Create(titleblock)`

Creates a new sheet.

The inputs are:

- `titleblock` (_FamilyType_) — The titleblock to use.

Returns `Sheet` (_Element_) — The newly created sheet.

Found in the library under **Create**.

Search terms: `sheet`, `rhythm`.

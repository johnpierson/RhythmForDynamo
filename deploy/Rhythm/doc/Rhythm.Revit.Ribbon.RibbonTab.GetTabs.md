## In Depth

`RibbonTab.GetTabs(toggle)`

This will give you access to all tabs.

The inputs are:

- `toggle` (_boolean_) — True or false to refresh collection.

Returns `ribbonTabs` (_list of RibbonTab_) — The ribbon tabs in the application.

Search terms: `Ribbon.RenameTab`.

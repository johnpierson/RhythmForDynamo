## In Depth

`SelFilter.GetLogicalAndFilter(first, second, executeAll: false)`

Creates a logical "and" filter

The inputs are:

- `first` (_ISelectionFilter_) — _Not documented yet._
- `second` (_ISelectionFilter_) — _Not documented yet._
- `executeAll` (_boolean_, defaults to `false`) — _Not documented yet._

Returns `result` (_ILogicalCombinationFilter_).

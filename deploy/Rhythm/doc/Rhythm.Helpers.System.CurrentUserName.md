## In Depth

`System.CurrentUserName(refresh: true)`

Returns the current windows user.

The inputs are:

- `refresh` (_boolean_, defaults to `true`) — Optional toggle to refresh the node

Returns `result` (_string_).

Found in the library under **Query**.

# Everything the documentation site is built with, pinned so that a build today and a build in a
# year produce the same pages. Installed by .github/workflows/docs.yml; install the same file
# locally to preview:
#
#   py -m venv .venv-docs
#   .venv-docs\Scripts\python -m pip install -r docs-requirements.txt
#   .venv-docs\Scripts\mkdocs serve
#
# To move a pin, bump it here and run `mkdocs build --strict` before committing. Strict mode is
# what the workflow runs, and it fails on a broken link, a bad heading anchor, or a page missing
# from the nav.
#
# Regenerate the whole file with `pip freeze` from a clean environment that has only
# mkdocs-material installed. The transitive dependencies are pinned as well, because
# "reproducible" that stops at the direct dependencies is not reproducible.

mkdocs==1.6.1
mkdocs-material==9.7.7

babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
ghp-import==2.1.0
idna==3.18
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
mergedeep==1.3.4
mkdocs-get-deps==0.2.2
mkdocs-material-extensions==1.3.1
packaging==26.2
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.0
Pygments==2.20.0
pymdown-extensions==11.0.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
urllib3==2.7.0
watchdog==6.0.0
